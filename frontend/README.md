# Mini ChatGPT Frontend (React + Vite + Tailwind)

## Setup

```bash
cd frontend
npm install
npm run dev
```

By default the app points to `http://localhost:8000`. Override with:

```bash
VITE_API_URL=http://localhost:8000 npm run dev
```

